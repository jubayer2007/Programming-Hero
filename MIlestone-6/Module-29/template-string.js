// kono string multiple line ee likhte chaile 'Backtick' die likhte hobe. For example:

const poem = `ata gas ee tuta pakhi
    dalim gase mou
    ato daki tobo kotha 
    kow na kn bow
    `
    console.log(poem);

// Another Example:

function sumOfNumbers(Num1, Num2) {
    const result = Num1 + Num2;
    const outPut = `sum of numbers ${Num1} and ${Num2} is ${result}`
    console.log(outPut);
    
}
sumOfNumbers(50, 200)

// Example

const price = 50;
const discountPrice =  `Discount Price of ${price} after 20% discount ${price * .8}`;
console.log(discountPrice);


    